// import express from 'express';
// import { treeController } from '../controllers/tree.controller';

// const treeRoutes = express.Router();

// // POST /api/tree  -  Create a new family tree
// treeRoutes.post('/tree', treeController.createNewTree);

// // GET /api/trees/:treeId -  Get single family tree
// treeRoutes.get('/trees/:treeId', treeController.getTreeById);

// // GET /api/trees  -  Get all family trees
// // treeRoutes.get('/trees', treeController.getUserTrees);

// export default treeRoutes;
